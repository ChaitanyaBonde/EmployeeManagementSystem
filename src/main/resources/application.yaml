spring:
  application:
    name: org-mgmt

  datasource:
    url: jdbc:mysql://localhost:3306/orgmgmt
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
